# 💻 Code Snippets & Testing Guide\n\n## How to Verify the Fixes\n\n### 1. Check Model Fields Were Added\n\n```bash\n# In VS Code terminal:\ncd e:\\accident_detection\\ (3)\\accident_detection\npython manage.py shell\n\n# In Django shell:\nfrom detection.models import VideoSession\nvs = VideoSession.objects.first()\nprint(f\"is_processed: {vs.is_processed}\")  # Should print: True or False\nprint(f\"alert_sent: {vs.alert_sent}\")      # Should print: True or False\nexit()\n```\n\n---\n\n### 2. Test Single Processing\n\n**Upload a video and refresh page MULTIPLE TIMES:**\n\n```\n1. Go to: http://localhost:8000/upload/\n2. Upload a test video\n3. IMMEDIATELY refresh page (Ctrl+R) 5-10 times\n4. Check Django console logs\n```\n\n**Expected Output in Logs:**\n```\nStarting CONTINUOUS VIDEO processing for video session 1\n✅ Marked video 1 as is_processed=True\n...[processing happens]...\n\nSecond attempt (you refreshed page):\n⏭️ VIDEO 1 ALREADY PROCESSED - SKIPPING (is_processed=True)\n```\n\n**Expected in Database:**\n```python\nvs = VideoSession.objects.get(id=1)\nprint(vs.is_processed)  # True (already processed)\nprint(vs.status)        # 'completed'\n```\n\n---\n\n### 3. Test Single Alert\n\n**Upload video and check accidents:**\n\n```python\n# After processing completes:\nfrom detection.models import Accident\naccidents = Accident.objects.filter(video_id='video_001')\nprint(f\"Total accidents: {accidents.count()}\")  # Should be 0 or 1, NEVER 2+\n\n# Check if alert was sent:\nvs = VideoSession.objects.get(video_id='video_001')\nprint(f\"Alert sent: {vs.alert_sent}\")  # True if accident detected\n```\n\n---\n\n### 4. Check Frame Counter NOT Updated\n\n**Upload a video:**\n\n```python\n# During processing:\nvs = VideoSession.objects.get(id=1)\nprint(vs.processed_frames)  # Should be ZERO (not updating per frame)\nprint(vs.status)            # Will be 'processing' or 'completed'\n```\n\n**Expected:** `processed_frames` stays 0 (not updating every 30 frames)\n\n---\n\n### 5. Verify UI Changes\n\n**After upload, you should see:**\n\n```\nSTATUS BEFORE FIX:\n❌ \"Processing: 240/301 frames processed\"\n❌ \"Completed: 301/301 frames analyzed\"\n\nSTATUS AFTER FIX:\n✅ \"⏱️ Processing Video... Please wait while the system analyzes your video.\"\n✅ \"✅ Processing Complete Video analysis finished.\"\n```\n\n**Check in browser DevTools:**\n```javascript\n// F12 → Console → You should NOT see:\n// - startProcessing() function calls\n// - \"Start Processing Now\" button\n\n// You SHOULD see:\n// - updateDetectionStatus() polling every 5 seconds\n// - Simple status messages without numbers\n```\n\n---\n\n## Important Code Locations\n\n### Model Fields (models.py - Lines ~88-90)\n```python\nclass VideoSession(models.Model):\n    # ... existing fields ...\n    status = models.CharField(max_length=20, choices=STATUS_CHOICES, default='pending')\n    is_processed = models.BooleanField(default=False)     # ← NEW\n    alert_sent = models.BooleanField(default=False)       # ← NEW\n    start_time = models.DateTimeField(null=True, blank=True)\n```\n\n### Safety Check (views.py - Lines ~54-60)\n```python\ndef _process_video_background(video_session_id, use_simple_detection=False):\n    video_session = VideoSession.objects.get(id=video_session_id)\n    \n    # SAFETY CHECK: Skip if already processed\n    if video_session.is_processed:  # ← CHECK THIS\n        logger.info(f\"⏭️ VIDEO {video_session_id} ALREADY PROCESSED - SKIPPING\")\n        return\n    \n    # Mark as processed IMMEDIATELY\n    video_session.is_processed = True  # ← SET THIS NOW\n    video_session.save()\n```\n\n### Alert Sending (views.py - Lines ~195-225)\n```python\n# SAVE RESULTS AND SEND ALERT - SINGLE ALERT PER VIDEO\nif accident_detected and not video_session.alert_sent:  # ← CHECK THIS\n    # ... create accident ...\n    \n    # Mark alert as sent\n    video_session.alert_sent = True  # ← SET THIS NOW\n    video_session.save()\n    logger.info(f\"✅ Alert sent flag set to True for video {video_session_id}\")\n```\n\n### Frame Update Removed (views.py - Lines ~110-115)\n```python\n# BEFORE: Updated frame counter\nif frame_count % 30 == 0:\n    video_session.processed_frames = frame_count  # REMOVED\n    video_session.save()                           # REMOVED\n    logger.info(f\"Progress...\")                    # REMOVED\n\n# AFTER: Only log, don't update database\nif frame_count % 50 == 0:\n    logger.debug(f\"Processing frame {frame_count}/{total_frames}\")  # Debug only\n```\n\n---\n\n## Database Migration Check\n\n**See the new fields in database:**\n\n```bash\n# Check migrations were applied:\npython manage.py showmigrations detection\n\n# Should show:\n[X] 0004_videosession_alert_sent_videosession_is_processed\n```\n\n**View all VideoSession records:**\n\n```python\n# In Django shell:\nfrom detection.models import VideoSession\n\nfor vs in VideoSession.objects.all():\n    print(f\"ID: {vs.id}\")\n    print(f\"  is_processed: {vs.is_processed}\")\n    print(f\"  alert_sent: {vs.alert_sent}\")\n    print(f\"  status: {vs.status}\")\n    print(f\"  processed_frames: {vs.processed_frames}\")\n    print()\n```\n\n---\n\n## Testing Workflow\n\n### Phase 1: Basic Upload\n```\n1. Start server: python manage.py runserver\n2. Open: http://localhost:8000/upload/\n3. Upload a video\n4. ✅ Check: Status shows \"Processing Video...\" (no frame count)\n5. ✅ Check: Button gone (\"Start Processing Now\" removed)\n```\n\n### Phase 2: No Duplicate Processing\n```\n1. Refresh page MULTIPLE times while processing\n2. Check logs for: \"ALREADY PROCESSED - SKIPPING\"\n3. ✅ Should appear on 2nd attempt onwards\n4. Database should show: is_processed=True\n```\n\n### Phase 3: Check Alert (if accident detected)\n```\n1. Wait for processing to complete\n2. If accident detected:\n   - Check database: Accident.objects.count() → Should be 1\n   - Check database: VideoSession.alert_sent → Should be True\n3. ✅ Only ONE accident record created\n4. ✅ Only ONE alert email (if configured)\n```\n\n---\n\n## Common Issues & Solutions\n\n### Issue: \"is_processed\" field doesn't exist\n```\nSolution:\n1. python manage.py makemigrations\n2. python manage.py migrate\n3. Restart server\n```\n\n### Issue: Still seeing \"240/301 frames\"\n```\nSolution:\n1. Clear browser cache (Ctrl+Shift+Delete)\n2. Refresh with Ctrl+F5 (hard refresh)\n3. Check upload_success.html was updated\n```\n\n### Issue: Multiple alerts still being sent\n```\nSolution:\n1. Check: video_session.alert_sent field exists\n2. Check: Migration was applied\n3. Check: Line \"if accident_detected and not video_session.alert_sent:\" exists\n4. Restart server\n```\n\n### Issue: Can still click \"Start Processing\" button\n```\nSolution:\n1. Check upload_success.html - button code should be removed\n2. Check startProcessing() function should be removed\n3. Clear browser cache\n4. Hard refresh (Ctrl+F5)\n```\n\n---\n\n## Debugging Commands\n\n```python\n# See the LAST video session:\nfrom detection.models import VideoSession\nvs = VideoSession.objects.latest('id')\nprint(f\"Status: {vs.status}\")\nprint(f\"Is Processed: {vs.is_processed}\")\nprint(f\"Alert Sent: {vs.alert_sent}\")\nprint(f\"Processed Frames: {vs.processed_frames}\")\n\n# See accidents for that video:\nfrom detection.models import Accident\nacc = Accident.objects.filter(video_id=vs.video_id)\nprint(f\"Accidents: {acc.count()}\")\nfor a in acc:\n    print(f\"  - {a.location} ({a.confidence_score:.1f}%)\")\n\n# Reset a video for testing:\nvs = VideoSession.objects.get(id=1)\nvs.is_processed = False\nvs.alert_sent = False\nvs.save()\nprint(\"Reset video for re-testing\")\n```\n\n---\n\n## Expected Behavior Summary\n\n| Action | Expected Result |\n|--------|------------------|\n| Upload video | Processing starts automatically |\n| Refresh page during processing | Processing continues (is_processed prevents restart) |\n| Processing completes | Status shows \"Processing Complete\" |\n| Accident detected | ONE Accident record created, alert_sent=True |\n| Wait for email | ONE email sent (if configured) |\n| Refresh page after completion | No re-processing (is_processed=True) |\n| Check processed_frames | Stays 0 (not updated frame-by-frame) |\n| Check UI | No \"240/301 frames\" message |\n\n---\n\n## Files to Check\n\n```\n✅ detection/models.py\n   - VideoSession has is_processed field\n   - VideoSession has alert_sent field\n   \n✅ detection/views.py\n   - _process_video_background() checks is_processed at start\n   - Sets is_processed=True immediately\n   - Checks video_session.alert_sent before sending\n   - Sets alert_sent=True after sending\n   - Removed frame counter updates to database\n   \n✅ detection/templates/upload_success.html\n   - No \"Start Processing Now\" button\n   - No startProcessing() function\n   - Status shows simple text (no frame count)\n   \n✅ detection/migrations/0004_*.py\n   - Migration file created and applied\n```\n\nAll checks complete! ✅\n